import { StarLevelDirective } from './star-level.directive';

describe('StarLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new StarLevelDirective();
    expect(directive).toBeTruthy();
  });
});
